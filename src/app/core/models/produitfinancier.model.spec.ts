import { Produitfinancier } from './produitfinancier.model';

describe('Produitfinancier', () => {
  it('should create an instance', () => {
    expect(new Produitfinancier()).toBeTruthy();
  });
});
